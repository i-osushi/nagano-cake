<div class="container mt-5 pb-5">
  <div class="form-group">
    <h2 class="mb-3 ml-5 text-center">商品詳細</h2>
    <div class="row mt-5 mb-5">
      <div class="col-lg-5">
       <%= image_tag @item.get_image(400,350), class: "rounded-bottom" %>
      </div>

      <div class="col-lg-7 ">
      <table class="table mx-auto">
        <tbody>
          <tr>
            <td>商品名</td>
            <td><%= @item.name %></td>
          </tr>

          <tr>
             <td>商品説明</td>
             <td><%= @item.introduction %></td>
          </tr>

          <tr>
            <td>ジャンル</td>
            <td><%= @item.genre.name %></td>
          </tr>

          <tr>
             <td>税込価格</br>（税抜価格）</td>
             <td><%= @item.add_tax_price.to_s(:delimited) %>（<%= @item.price.to_s(:delimited) %>）円</td>
          </tr>

           <tr>
             <td>販売ステータス</td>
             <td>
               <% if @item.is_active == true %>
                      <p class="text-success"><strong>販売中</strong></p>
               <% else %>
                      <p class="text-secondary"><strong>販売停止中</strong></p>
               <% end %>
             </td>
           </tr>
        </tbody>
       </table>
      </div>
      </div>
      <div class="text-center">
       <%= link_to "編集する", edit_admin_item_path, class: "btn btn-success mt-3 ml-5 px-4 py-2" %>
      </div>
    </div>
  </div>
</div>